<Window x:Class="InputHistory.MainWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:InputHistory"
			xmlns:properties="clr-namespace:InputHistory.Properties"
				mc:Ignorable="d"
      Title="InputHistory" Height="450" Width="800" Loaded="Window_Loaded"
			FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=FontName}"
			FontSize="{Binding Source={x:Static properties:Settings.Default},   Path=FontSize}">
	<Window.ContextMenu>
		<ContextMenu>
			
		</ContextMenu>
	</Window.ContextMenu>
	<Grid>
		<StackPanel x:Name="HistoryContainer" Orientation="Horizontal" HorizontalAlignment="Right">

		</StackPanel>
	</Grid>
</Window>
